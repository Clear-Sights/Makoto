---
description: Show makoto's integrity-guardrail state (read-only)
allowed-tools: Bash(python3 -m makoto status)
disable-model-invocation: true
---

## Makoto status

!`python3 -m makoto status`

The above is read-only: install state, hooks wired, state dir, and any patterns
muted via `MAKOTO_DISABLE_PATTERNS`. No changes are made by this command.
