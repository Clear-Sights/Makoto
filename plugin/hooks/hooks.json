{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "if": "Write(**/CLAUDE.md)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B7",
            "prompt": "makoto B7 RULE WITH NO RUNNER: the edit adds a rule (must / never / always / every) to an instructions file and names no check, test, hook or script that enforces it. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B7 RULE WITH NO RUNNER: bind every rule to a check on every path -- name the check that runs this rule, or add one.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Edit(**/CLAUDE.md)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B7",
            "prompt": "makoto B7 RULE WITH NO RUNNER: the edit adds a rule (must / never / always / every) to an instructions file and names no check, test, hook or script that enforces it. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B7 RULE WITH NO RUNNER: bind every rule to a check on every path -- name the check that runs this rule, or add one.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Write(**/AGENTS.md)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B7",
            "prompt": "makoto B7 RULE WITH NO RUNNER: the edit adds a rule (must / never / always / every) to an instructions file and names no check, test, hook or script that enforces it. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B7 RULE WITH NO RUNNER: bind every rule to a check on every path -- name the check that runs this rule, or add one.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Edit(**/AGENTS.md)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B7",
            "prompt": "makoto B7 RULE WITH NO RUNNER: the edit adds a rule (must / never / always / every) to an instructions file and names no check, test, hook or script that enforces it. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B7 RULE WITH NO RUNNER: bind every rule to a check on every path -- name the check that runs this rule, or add one.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "if": "Write(**/*check*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B10",
            "prompt": "makoto B10 NEVER-ABSTAINING CHECK: the edit adds or widens a check/detector whose condition would also fire on ordinary benign input (it fires on everything, so it says nothing), and no benign case it stays silent on is shown. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B10 NEVER-ABSTAINING CHECK: measure its rate on benign input -- show an ordinary input it stays silent on.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Edit(**/*check*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B10",
            "prompt": "makoto B10 NEVER-ABSTAINING CHECK: the edit adds or widens a check/detector whose condition would also fire on ordinary benign input (it fires on everything, so it says nothing), and no benign case it stays silent on is shown. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B10 NEVER-ABSTAINING CHECK: measure its rate on benign input -- show an ordinary input it stays silent on.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "if": "Write(**/test_*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B21",
            "prompt": "makoto B21 OVERDETERMINED VERDICT: the edit adds a test whose input trips more than one condition, so the asserted verdict would still hold if the condition the test names were removed (a sibling condition gives the same answer). Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B21 OVERDETERMINED VERDICT: isolate each condition with its own input -- give the named condition an input no sibling condition trips.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Edit(**/test_*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B21",
            "prompt": "makoto B21 OVERDETERMINED VERDICT: the edit adds a test whose input trips more than one condition, so the asserted verdict would still hold if the condition the test names were removed (a sibling condition gives the same answer). Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B21 OVERDETERMINED VERDICT: isolate each condition with its own input -- give the named condition an input no sibling condition trips.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "if": "Write(**/*check*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B34",
            "prompt": "makoto B34 LAW EXEMPTS ITS INSTRUMENT: the edit exempts the checker's own files, directory or machinery from the very rule the checker enforces (a skip, allowlist or path exclusion naming itself), without naming that region as unreached. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B34 LAW EXEMPTS ITS INSTRUMENT: run the artifact's own law over the artifact; name every region it cannot.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          },
          {
            "type": "prompt",
            "if": "Edit(**/*check*.py)",
            "continueOnBlock": true,
            "timeout": 30,
            "statusMessage": "makoto B34",
            "prompt": "makoto B34 LAW EXEMPTS ITS INSTRUMENT: the edit exempts the checker's own files, directory or machinery from the very rule the checker enforces (a skip, allowlist or path exclusion naming itself), without naming that region as unreached. Judge only this one tool call. If the rule clearly applies and is broken, respond {\"ok\": false, \"reason\": \"makoto B34 LAW EXEMPTS ITS INSTRUMENT: run the artifact's own law over the artifact; name every region it cannot.\"}. If it does not apply, or you are unsure, respond {\"ok\": true}. Input: $ARGUMENTS"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/makoto/_dispatch_shim.sh\""
          }
        ]
      }
    ]
  }
}
