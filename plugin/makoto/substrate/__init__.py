"""makoto.substrate — neutral leaf home for op-restricted content-identity primitives (CANON-PORT-1).

Intentionally an EMPTY package init (no eager imports), so importing makoto.substrate.byte_identity
triggers ZERO package-init side effects — the same discipline the ancestor used to keep the canon
import closure clean."""
